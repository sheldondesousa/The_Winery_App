# Find Specification

The earlier Guided Selection draft is superseded by [PRD Section 6A](PRD.md#6a-find), including the supplied three-column multi-select design.

Every option cell is fully clickable, including its circular check indicator, label, and whitespace. Multiple values are alternatives within each category; categories combine as constraints.
